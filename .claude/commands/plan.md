## Task: Plan (do NOT write any code yet) — $ARGUMENTS

Use plan mode (Shift+Tab). Think deeply before proposing anything.

Scan the relevant parts of the codebase, then deliver:

## 1. Files to CREATE
[path] → [one-line purpose]

## 2. Files to MODIFY
[path] → [what changes and why]

## 3. New packages required
[package@version] → [why needed]
— If none needed, say so explicitly.

## 4. Migration / CLI commands to run
[print the exact command — NEVER auto-run]

## 5. Risks & ambiguities
[anything unclear that needs my decision before you proceed]

## 6. Estimated scope
[S / M / L — and why]

---
Wait for explicit approval before writing a single line of code.
If anything is ambiguous, ask ONE clarifying question now.
